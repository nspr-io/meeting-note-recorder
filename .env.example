# Google Calendar OAuth Credentials (required for calendar sync)
# Create OAuth credentials at https://console.cloud.google.com
GOOGLE_CLIENT_ID=your_google_oauth_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_oauth_client_secret

# Recall.ai API Configuration (required for recording/transcription)
RECALL_API_KEY=your_recall_api_key
RECALL_API_URL=https://us-west-2.recall.ai

# Anthropic API (optional, for real-time coaching features)
ANTHROPIC_API_KEY=your_anthropic_api_key

# Notion integration (optional)
NOTION_INTEGRATION_TOKEN=your_notion_integration_token
NOTION_DATABASE_ID=your_notion_database_id
NOTION_TODO_INTEGRATION_TOKEN=your_notion_todo_token
NOTION_TODO_DATABASE_ID=your_notion_todo_database_id

# Logging Configuration
# Options: error, warn, info, debug
# error = only errors in console
# warn = errors + warnings in console
# info = errors + warnings + info in console (default for dev)
# debug = all logs in console (very verbose)
LOG_LEVEL=info